[alias]
    s = status
    d = diff
    ds = diff --staged
    du = diff @{u} HEAD
    co = checkout
    cob = checkout -b
    cp = cherry-pick
    fa = fetch --all
    sw = switch
    amend = commit --amend
    fixup = commit --fixup
    squash = commit --no-edit --squash
    irebase = rebase --interactive
    unstage = restore --staged
    toss = restore --staged --worktree
    yeet = reset @{u}
    yeet-hard = reset --hard @{u}

[user]
    name = Maximilian Moser
    email = maximilian.moser@tuwien.ac.at

[core]
    pager = "delta || ${PAGER:-less}"

[status]
    branch = true
    showStash = true

[branch]
    sort = -committerdate

[tag]
    sort = -taggerdate

[blame]
    ignoreRevsFile = :(optional).git-blame-ignore-revs

[diff]
    renames = copies
    colorMoved = dimmed-zebra
    colorMovedWS = allow-indentation-change

[commit]
    verbose = true

[push]
    autoSetupRemote = true

[pull]
    rebase = true

[merge]
    conflictstyle = diff3

[rebase]
    autoStash = true
    autoSquash = true
    updateRefs = true
    missingCommitsCheck = warn

[interactive]
    singleKey = true
    diffFilter = "delta --color-only || ${PAGER:-less}"
    side-by-side = true

[rerere]
    enabled = true

[color "diff"]
    # colors used by `diff.colorMoved = zebra`
    # these are the default values, but set again here for reference
    # (they get mapped in the delta theme)
    oldMoved = "bold magenta"  # note: this gets picked up as "bold purple" by delta
    oldMovedAlternative = "bold blue"
    newMoved = "bold cyan"
    newMovedAlternative = "bold yellow"

[include]
    path = delta
    path = config.local
    path = ~/.gitconfig.local
